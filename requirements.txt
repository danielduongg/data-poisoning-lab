# Core (required for the offline, reproducible demo)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Optional: load real corpora to validate the pipeline beyond synthetic data
# (scikit-learn's fetch_20newsgroups, etc. require network access)
